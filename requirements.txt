# Backend
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.14.2

# Database
aiosqlite>=0.22.1

# Vector DB
qdrant-client>=1.12.0

# LLM
anthropic>=0.39.0
# OpenRouter / Gemini-compatible client used by llm_client (ADR 0007)
openai>=1.40

# Embedding (bge-m3) via the local Ollama daemon (pull with `ollama pull bge-m3`).
# The sentence-transformers backend + CrossEncoder reranker are OPTIONAL and live
# in requirements-st.txt (they pull torch — kept out of the slim image).
ollama>=0.4.0

# Lexical search (BM25) — jieba segments CJK so FTS5 unicode61 gets real words
jieba>=0.42.1

# TMDb
httpx>=0.27.0

# Observability — Prometheus metrics exposition (/metrics). Pure-python, tiny;
# always installed so metrics work on the slim image too.
prometheus-client>=0.21.0

# Frontend
nicegui>=2.9.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-httpx>=0.36.2

# Utils
python-dotenv>=1.0.1

# Eval-only deps (ranx etc.) live in requirements-eval.txt — they pull
# C-extension packages (zlib-state via ir-datasets) with no arm64 wheels, so
# keeping them out of the runtime image lets it build multi-arch (amd64+arm64).
# Run the search eval with: pip install -r requirements-eval.txt

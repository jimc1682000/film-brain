# Search Replay

Complete outputs of the **real pipeline** (LLM query understanding → two-track recall → wildcards → cross-encoder rerank → honest scores), captured as JSON and replayed — the static site has no backend, so only the preset queries are clickable. Scores, warnings and explainability labels are the genuine outputs from capture time. Queries and film data are in Chinese (the product's market).

<SearchReplay />
